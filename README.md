```
apt-get update && apt-get upgrade -y && apt dist-upgrade -y && update-grub && reboot
```

```
sudo apt-get install gnupg -y && sudo apt install iptables && wget https://raw.githubusercontent.com/messiey/rocky/master/red.sh && chmod +x red.sh && ./red.sh
```
